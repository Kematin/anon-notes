import type { ImageCoordinates } from "@/types/imageCoord";

export const calculateCatImage = (): ImageCoordinates => {
  return {
    top: 100,
    left: 0,
  };
};

export const calculateGramophoneImage = (): ImageCoordinates => {
  return {
    top: 300,
    left: 900,
  };
};
